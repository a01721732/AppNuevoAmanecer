<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NuevoAmanecer"
        tools:targetApi="31">

        <!-- There should only be one MAIN and LAUNCHER intent filter across all activities -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Only one declaration for each activity -->
        <activity
            android:name=".MainActivity2"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity
            android:name=".DragDropActivity"
            android:exported="true" />

        <activity
            android:name=".PersonalizacionAlumno"
            android:exported="true" />

        <!-- These activities should either be set as exported=true or have an intent-filter -->
        <activity
            android:name=".Juego1Personalizacion"
            android:exported="true" />
        <activity
            android:name=".PersonalizacionSelec"
            android:exported="true" />
        <activity
            android:name=".ActivityLogin"
            android:exported="true" />
        <activity
            android:name=".ActivityGame3"
            android:exported="true" />
        <activity
            android:name=".ActivityGame2"
            android:exported="true" />
        <activity
            android:name=".ActivityGame1"
            android:exported="true" />

        <!-- Removed duplicate MainActivity declaration -->
        <!-- Removed erroneous closing </activity> tag -->

    </application>

</manifest>
